# Example headers.yaml file
# Replace these values with your actual LinkedIn request headers
# 
# To capture fresh headers:
# 1. Open LinkedIn Campaign Manager in your browser
# 2. Open Developer Tools (F12)
# 3. Go to Network tab
# 4. Make an audience search request
# 5. Find the campaignManagerAudienceCounts request
# 6. Copy the headers from the request

accept: "application/vnd.linkedin.normalized+json+2.1"
accept-language: "en-US,en;q=0.9"
csrf-token: "your-csrf-token-here"
# Add other headers as needed
